---
- name: Dev Container Fortress host bootstrap
  hosts: localhost
  connection: local
  gather_facts: true

  roles:
    - role: homebrew
    - role: python_uv
    - role: terraform
    - role: shell_config
    - role: tmux

