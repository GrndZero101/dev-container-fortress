# M0 Shell and Container Baseline

## Status

- [x] Milestone complete

## Objective

Establish the repo structure, container targets, shell-config integration, and
the first usable `ft` CLI baseline.

## Exit Criteria

- [x] Repo structure exists for host, container, and devcontainer work
- [x] Ubuntu and Alpine disposable targets exist
- [x] `ft` CLI baseline is packaged and usable
- [x] `shell-config` integration exists for container builds
- [x] `just` is available as a thin front door

## Delivered Scope

- [x] `ft` operator CLI foundation
- [x] Ubuntu and Alpine disposable container targets
- [x] `shell-config` integration in container builds
- [x] Initial validation and completion workflows
- [x] `just` as a thin shim over the CLI

## Issue Drafts

- [x] Establish initial repository layout
- [x] Add Ubuntu container target
- [x] Add Alpine container target
- [x] Package the first `ft` CLI baseline
- [x] Wire `shell-config` into container builds

## Verification Notes

- [x] Baseline contributor workflows were established
- [x] Container targets and CLI scaffolding were proven usable
